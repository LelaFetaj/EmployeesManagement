namespace EmployeeManagementAPI.Controllers
{
    public class UserProfileController
    {
        // POST /api/userprofiles/{userId} employee his, admin all
        // GET /api/userprofiles admin
        // GET /api/userprofiles/{id} employee his, admin all
        // PUT /api/userprofiles/{id} employee his, admin all
        // DELETE /api/userprofiles/{id} admin
        // PUT /api/update-profile-picture/{userId} employee his, admin all
        
    }
}